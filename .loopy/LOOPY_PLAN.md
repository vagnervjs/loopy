---
agent_command: cursor-agent
test_command: ""
max_iterations: 50
max_minutes: 120
backoff_ms: 5000
rotate_bytes: 150000
git:
  branch: loopy/cli-refactor
  commit: false
  commit_message: "loopy: {change_type} {task_summary}"
phase_defaults:
  stop_on: all_checked
  test_command: npm test
phases:
  - id: cli-flag-surface
    title: Update CLI flag surface and remove legacy options
    stop_on: all_checked
    test_command: npm test
  - id: plan-terminology
    title: Update help text to Plan terminology
    stop_on: all_checked
    test_command: npm test
  - id: file-layout
    title: Move generated files into .loopy/
    stop_on: all_checked
    test_command: npm test
  - id: prompts-and-defaults
    title: Add missing-config prompts and defaults
    stop_on: all_checked
    test_command: npm test
  - id: docs-and-examples
    title: Refresh docs and examples for the new surface
    stop_on: all_checked
    test_command: npm test
---

# Plan

<!-- loopy:seed # PRD: CLI Flag Renames and Legacy Cleanup

## Problem Statement
The CLI still exposes legacy flags and docs that no longer match the desired UX, causing confusion and inconsistent usage.

## Goals
- Align the CLI with the new flag names and plan terminology.
- Remove deprecated/legacy options and commands to simplify UX.
- Update documentation so it reflects the current CLI surface.

## Non-Goals
- Introduce new features unrelated to flag/command renaming or cleanup.

## Requirements (high level)
- CLI uses `--plan` for plan doc path.
- CLI uses `--agent` for agent command override.
- Autogenerated prompt output defaults to `.loopy/PROMPT.md`.
- Default plan file path is `.loopy/LOOPY_PLAN.md`.
- If `agent_command` is missing, prompt before running.
- If no test command is configured, prompt before running.
- If `git.branch` is missing, auto-generate one using the `loopy/` prefix.
-->

## Phase: cli-flag-surface
<!-- loopy:phase cli-flag-surface -->

- [x] Switch CLI to `--plan` for plan doc path (no legacy compatibility).
- [x] Switch CLI to `--agent` for agent override (no legacy compatibility).
- [x] Remove legacy/deprecated flags and commands from parsing logic.
- [x] Ensure removed legacy flags/commands do not appear in errors or usage output.

## Phase: plan-terminology
<!-- loopy:phase plan-terminology -->

- [x] Update CLI help/usage text to use “Plan doc” terminology.
- [x] Update option descriptions to reference `--plan` and the plan file location.
- [x] Verify CLI help output contains no legacy flag references.

## Phase: file-layout
<!-- loopy:phase file-layout -->

- [x] Set prompt output default to `.loopy/PROMPT.md`.
- [x] Set plan file default to `.loopy/LOOPY_PLAN.md`.
- [x] Keep generated artifacts inside `.loopy/` unless explicitly overridden.
- [x] Update path-related messaging to reflect `.loopy/` locations.

## Phase: prompts-and-defaults
<!-- loopy:phase prompts-and-defaults -->

- [x] If `agent_command` is missing, prompt the user to define it before running.
- [x] If no test command is configured, prompt the user to define it before running.
- [x] If `git.branch` is missing, auto-generate a branch name using the `loopy/` prefix.
- [x] Confirm these prompts/defaults occur before executing agent/test actions.

## Phase: docs-and-examples
<!-- loopy:phase docs-and-examples -->

- [x] Update `/examples` to match the new flags and `.loopy/` file locations.
- [x] Update docs/README/PRD guides to reflect the current CLI surface and paths.
- [x] Add a short migration note highlighting breaking removals (no compatibility).
- [x] Re-check docs and examples for zero legacy flag references.

